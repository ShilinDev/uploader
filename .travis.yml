language: php

php:
  - 7.2

before_script:
  - cd image-uploader
  - cp .env.travis .env
#  - mysql -e 'create database homestead_test;'
  - composer install --no-interaction
  - php artisan key:generate
  - php artisan migrate

script:
  - phpunit

services:
  - mysql

branches:
  only:
    - master
